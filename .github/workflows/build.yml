name: Build
on: [push]
jobs:
  build_latex:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v1
      - name: Compile Lastenheft.tex
        uses: dante-ev/latex-action@master
        with:
          working-directory: Pflichtenheft/
          root_file: Lastenheft.tex
      - name: Compile Entwurf.tex
        uses: dante-ev/latex-action@master
        with:
          working-directory: Entwurf/
          root_file: Entwurf.tex
      - name: Upload Pflichtenheft
        uses: actions/upload-artifact@master
        with:
          name: Pflichtenheft
          path: Pflichtenheft/Lastenheft.pdf
      - name: Upload Entwurf
        uses: actions/upload-artifact@master
        with:
          name: Entwurf
          path: Entwurf/Entwurf.pdf